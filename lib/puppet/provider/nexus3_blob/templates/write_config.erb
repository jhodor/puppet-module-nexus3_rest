def blobName = '<%= resource[:blob_name] %>'
def blobPath = '<%= resource[:blob_path] %>'
def blobStoreManager = blobStore.getProperty('blobStoreManager')
def blobExist = blobStoreManager.exists(blobName)
if ( ! blobExist ) { def rawStore = blobStore.createFileBlobStore(blobName, blobPath) }
